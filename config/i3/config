# --- tp01 i3 Config (Debian 13 + Polybar) ---
# Modern Visuals | Classic Workflow | Polybar

set $mod Mod4

# 1. Visuals: Fonts & Gaps
# Modern Nerd Font for icons
font pango:JetBrains Mono Nerd Font 10

# Clean borders (no title bars)
default_border pixel 2
default_floating_border pixel 2

# Gaps (Breathing room)
gaps inner 10
gaps outer 5

# 2. Autostart (Desktop Essentials)
# Transparency & Fades
exec_always --no-startup-id picom -b

# POLYBAR LAUNCHER
# Replaces the default i3bar. Ensure your launch script handles your dual monitors.
exec_always --no-startup-id ~/.config/polybar/launch.sh

# Network & Bluetooth (Debian 13)
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet

# Deskflow KVM
exec --no-startup-id deskflow

# Polkit Auth Agent (Debian 13 Path)
exec --no-startup-id /usr/libexec/polkit-gnome-authentication-agent-1

# Power Manager
exec --no-startup-id xfce4-power-manager

# Wallpaper (Restore last wallpaper)
exec --no-startup-id nitrogen --restore

# xss-lock
exec --no-startup-id xss-lock -- i3lock -n -c 000000

# 3. Sound Controls
set $refresh_i3status killall -SIGUSR1 i3status
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10%
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10%
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# 4. Standard Keybindings (n7110 Muscle Memory)
floating_modifier $mod
tiling_drag modifier titlebar

# Terminals & Windows
bindsym $mod+Return exec xfce4-terminal
bindsym $mod+Shift+q kill

# Launcher (Rofi with Icons)
bindsym $mod+d exec --no-startup-id rofi -show drun -show-icons

# Focus (Vim keys)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move Windows
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Layouts
bindsym $mod+b split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# --- Dual Monitor Configuration (Optional) ---
# Move the current active workspace to the other monitor
bindsym $mod+Control+Left move workspace to output left
bindsym $mod+Control+Right move workspace to output right

# Alternatively, a single key to bounce the workspace to the other available screen
bindsym $mod+o move workspace to output next


# Uncomment and adjust outputs (run 'xrandr' to find names like DP-0 or HDMI-0)
# workspace $ws1 output DP-0
# workspace $ws10 output HDMI-0
exec_always xrandr --output HDMI-0 --primary --rotate left --output DP-0 --auto --left-of HDMI-0
# In ~/.config/i3/config
bindsym $mod+Shift+m exec --no-startup-id /home/kevan/custom_scripts/monitor_setup.sh
# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# --- Workspaces 11-20 (Secondary Monitor) ---


# System Control
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Exit i3?' -B 'Yes' 'i3-msg exit'"

# Resize Mode
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Screenshots
bindsym Print exec --no-startup-id scrot -u /tmp/screenshot.png -e 'xclip -selection clipboard -t image/png -i $f && rm $f'

# Scratchpad
bindcode $mod+20 scratchpad show
bindcode $mod+Shift+20 move scratchpad

# --- Special Rules ---
# Dashboard (Matches XFCE Terminal Title)
for_window [title="monitor"] move to workspace 9
exec --no-startup-id xfce4-terminal --title="monitor" --command="btop"

for_window [class="Virt-manager"] move to workspace 10
# Cheatsheets
#for_window [instance="cheat"] move to workspace 9
#exec --no-startup-id firefox --name "cheat" https://i3wm.org/docs/refcard.html

# Note: The 'bar {}' block has been removed to allow Polybar to take over.

# Laptop Brightness Control (F3 & F4)
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Audio Volume Control (F9, F10, F11)
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%

